DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=foodlink_db
JWT_SECRET=foodlink_super_secret_key_change_this
PORT=5000

# Needed for forgot-password.js (Gmail SMTP via nodemailer).
# MAIL_USER = the Gmail address that sends the reset email.
# MAIL_PASS = a Gmail App Password (NOT your normal Gmail password) -
#             generate one at https://myaccount.google.com/apppasswords
# Without these two, "Forgot Password" will 500 when it tries to send.
MAIL_USER=hasinipothanaboyina0@gmail.com
MAIL_PASS=mzum sixx whtj iwdy

# Used to build the reset link in the password-reset email.
# Defaults to localhost:5000 if unset (fine for local dev).
FRONTEND_URL=http://localhost:5000